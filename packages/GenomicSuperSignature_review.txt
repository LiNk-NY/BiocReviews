# GenomicSuperSignature #2003

## DESCRIPTION

* Consider cutting down on the number of `Suggest`ed packages.
* Everything else looks good.

## NAMESPACE

* It looks good.

## vignettes

* Change the installation instructions to use `BiocManager`.
* Avoid installing packages in the vignette, simply use a `library` call to
load the package. These dependencies are stated in the `Suggests` field of
your `DESCRIPTION` file.
* `eval=FALSE` chunks in the Quickstart vignette should be evaluated or removed

## R

* Don't include manual outputs in your examples (`R/annotatePCcluster.R` and
others). Perhaps you can use these as templates for unit tests.
* To reduce the amount of repeated code, isolate the difference in the
conditions and make that the content of the `if` `else` statements, for
example in `R/annotatePCcluster.R`:
```r
FUN <- if (abs) { abs } else { I }
```
and then do:
```r
topAnnotation <- annotatedCluster[
    order(FUN(annotatedCluster$NES), decreasing = TRUE),,drop = FALSE][1:n,]
```
* Avoid using `1:n` sequence generation and use the more robust `seq_len(n)`.
* Test S4 class membership with `is(RAVmodel, "PCAGenomicSignatures")` from
the `methods` package. For S3, use `is.list` (`R/buildAvLoading.R`).
* Describe what the default NULL behavior does in `findStudiesInCluster`.
* Avoid using copy-and-append methods with for loops, use the allocate and
fill method (`buildAvgLoading`).
* Use coercion rather than wrapping when checking class membership in
`calculateScore`, e.g.,
```
> list(List())
[[1]]
List of length 0

> as.list(List())
list()
```
* Using `get` in package code is not usually recommended (`commonGene`).
If you're trying to load data from the package create an environment for
depositing that data and extract it from there:
```r
    local_data_store <- new.env(parent = emptyenv())
    data("sampleTypes", envir = local_data_store, package = "TCGAutils")
    sampleTypes <- local_data_store[["sampleTypes"]]
```
* Avoid setting a seed inside a function (`drawWordCloud`).
* Use the more robust `vapply` over `sapply`.
* If you created methods for the virtual class (`GenomicSignatures`) you may
not need to create additional methods for `PCAGenomicSignatures` because
they're automatically inherited, unless those methods are different.
* Some methods are compatible out of the box, e.g., `colData` because you
are inheriting from `SummarizedExperiment` so you don't need to create them.
* State your `rappdirs` dependency in the Imports field or use
`tools::R_user_dir`.
* Perhaps run `.RAVName` before creating the `PCAGenomicSignatures` class
in the constructor function? Usually the class construction occurs after the
pre-processing steps.
* Some of the inputs in your class are matrix which perhaps should be your
class or even `SummarizedExperiment`? (`plotAnnotatedPCA`)
* Avoid using `class(x) %in% c("x", "y", "z")` constructions and use the S3
methods of `is.data.frame`, `is.matrix`, `is.list` (`subsetEnrichedPathways`).
* These functions can be reduced to one (`R/validatedSingatures.R`):
```r
.filterByClusterSize <- function(data, cutoff) {
  ind <- which(rownames(data) == "cl_size")
  val_ind <- which(data[ind,] >= cutoff)
  dat <- data[,val_ind]
  return(dat)
}

.filterByScore <- function(data, cutoff) {
  ind <- which(rownames(data) == "score")
  val_ind <- which(data[ind,] >= cutoff)
  dat <- data[,val_ind]
  return(dat)
}

.filterByAvgSW <- function(data, cutoff) {
  ind <- which(rownames(data) == "sw")
  val_ind <- which(data[ind,] >= cutoff)
  dat <- data[,val_ind]
  return(dat)
}
```

```r
.filterBy <- function(data, cutoff, type = c("cl_size", "score", "sw")) {
  type <- match.arg(type)
  ind <- rownames(data) == type
  val_ind <- which(data[ind,] >= cutoff)
  data[,val_ind]
}
```

Minor:
* You don't need to use `isTRUE`, `isFALSE`, or `x == TRUE` on expected
logical values. What you should do is check those inputs by doing something
like: `stopifnot(is.logical(abs), length(abs) == 1L, !is.na(abs))`.
* Note you can also use `utils::globalVariables` for line 29 in
`R/funcForGSEA.R`
* Stick to the 80 column width limits for your code.

## tests

* Add more tests to make the code more robust.

