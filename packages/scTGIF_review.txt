# scTGIF #1255

## README.md

* Please use a vector of packages as input to `BiocManager::install`

## DESCRIPTION

* Please format the file to fit the 80 column width limit
* It is recommended that the `Imports` field contain one package per line for
readability
* `Date` field is not really needed

## NAMESPACE

* It looks good.
* Consider using `roxygen2` for documentation

## vignettes

* Introduction well established.
* Avoid using `eval=FALSE` in chunks. Provide a smaller example dataset if the
current one is too big.

## R

* It is recommended that you do not create methods for classes that are not
your own especially if there is only one method. Please use a plain function
for `settingTGIF`.
* Avoid `eval` / `parse` operations and use alternatives such as `[[`.
* Avoid putting objects in the `.GlobalEnv` from an internal function. Use the
conventional return object.
* Do not save files / reports for the user by default, make this argument
optional.
* Use temporary files by default and display HTML pages
* Consider using `Rcpp` for `text.aMat`

