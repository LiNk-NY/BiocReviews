# Ularcirc #688

* Remove `docs` folder from git repository and add to `.gitignore`
* Ensure that your formatting is following recommendations. Keep lines of code
shorter than 80 column width. It makes code easier to maintain and review :)

## DESCRIPTION

* Looks good, you should format the `Description` and `Imports` fields so that
they fit under 80 column width.
* Minor: Author and Maintainer can be combined into the `Authors@R` field

## R

* Avoid using `1:length(x)` sequence generation. Use the more robust `seq_along`
and `seq_len`. 
* You may just want to provide a `data.frame` as the output of
`Compatible_Annotation_DBs` with 2 columns. The data type and the actual
name, e.g., `data.frame(dataType = "Annotations", name = "org.Bt.eg.db")`
* It's recommended that you use the allocate and fill method for creating
vectors rather than the copy and append (Line#63).
* I ran the shiny app and kept getting an error: 
`object 'idx' not found`. After selecing `Hsapiens.UCSC.hg19` in the setup
tab. Default "states" in other tabs should just work or have a default
message for additional input rather than an error.

Minor:
* `if(length(x) > 0)` is the same as `if(length(x))`

## inst/extdata

* The datasets contained in this folder are pretty big (~7.8 MB).
Can you reduce the size of these?
* Why not include the functions found in the shiny app directory in the
package?

## vignettes

* It looks good. Step 5 should be finished. 

## man

* It looks okay. Capitalize Bioconductor in `Compatible_*` man page.

