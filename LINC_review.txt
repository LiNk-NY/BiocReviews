#### Dependencies 
Your `DESCRIPTION` file states too many dependencies. It is recommended to selectively import from such packages via the `importFrom` directive. 
*Message*: 
```
Depends: includes the non-default packages:
  ggplot2 ReactomePA clusterProfiler reshape2 reshape org.Hs.eg.db
```
#### Getters and Setters
Your code shouldn't include any `@` slot accessors. You should create methods to access slots in your class so that something like `getter(object)` works. You might also want to include setReplaceMethod` so that you can replace values in the slots. 

#### Image files
I saw that you have multiple `*.png` files in the package. Do you need to include all of those? Are these used for plotting? 
```
>LINC
./inst/extdata/statslinc_img.png
./inst/extdata/singlelinc_img.png
./inst/extdata/protcl_img.png
./inst/extdata/clust_img.png
./inst/extdata/stclust_img.png
./inst/extdata/overview_img.png
./inst/extdata/interbio_img.png
./inst/extdata/barplot_img.png
./inst/extdata/querycluster_img.png
```

#### Vignette
It would be better to output the vignette as an HTML_document since it is more versatile and amenable to being read in R. 
